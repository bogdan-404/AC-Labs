services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: labdemo
      POSTGRES_USER: labdemo
      POSTGRES_PASSWORD: labdemo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
